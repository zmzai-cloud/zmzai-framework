---
"@zmzai/agent-framework": patch
---

权限「总是允许」沉淀规则安全收紧：bash/终端复合命令（管道、`;`、`&&`、命令替换）只沉淀精确整串、绝不下沉 `program *` 程序级通配（防 `npm x && rm -rf` 一次批准全放行）；MCP 工具批准只沉淀当前工具的精确规则，不再自动沉淀 `server/*` 服务器级通配（服务器间信任不应隐式传染，「信任整个服务器」应由产品层显式提供）。
